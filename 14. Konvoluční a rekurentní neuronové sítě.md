# Konvoluční neuronové sítě a jejich aplikace. Rekurentní neuronové sítě a jejich aplikace, modely typu LSTM a GRU.

## Konvoluční neuronové sítě

- Operace konvoluce je definována jako skalární součin jako:

  $$
  z_{uv} = \sum_{i=-k}^k \sum_{j=-k}^k w_{ij}x_{u+i,v+j}+b\\
  \Downarrow\\
  z_{uv} = \boldsymbol{w}^T\boldsymbol{x}_{uv}+b.
  $$

- Aby byl výstup konvoluce stejně velký jako vstup (pokud není konvoluční jádro velikosti $1$), je potřeba nastavit padding, např. zero padding.
- Filtr vždy zahrnuje všechny vstupní kanály, tj. např. u první vrstvy 3 kanály: R, G, B, výstupem je pak tzv. feature mapa.

$$
\boldsymbol{X} \in \mathbb{R}^{H \times W \times C},\boldsymbol{Z} \in \mathbb{R}^{Q \times Q \times F}, \\

\boldsymbol{W} \in \mathbb{R}^{K \times K' \times C \times F},\\
\boldsymbol{b} \in \mathbb{R}^{F},\\
$$

kde $K$ je hyperp. velikost konvolučního filtru, $F$ hyperp. počet filtrů, $P$ hyperp. je padding, $S$ je hyperp. stridem, $C$ je počet kanálů (fixní).

![CNN](assets/cnn.png)

$$
  \frac{\partial z_{uv}}{\partial w_{ijc}}=x_{su+i, sv+j, c}\\
  \frac{\partial L}{\partial w_{ijc}} = \sum_{u=1}^Q\sum_{v=1}^Q \frac{\partial L}{\partial z_{uv}} \frac{\partial z_{uv}}{\partial w_{ijc}}\\
  = \sum_{u=1}^Q\sum_{v=1}^Q \overline{z_{uv}} x_{su+i, sv+j, c},
$$

zpětná propagace gradientu na vstup konvoluce je tedy opět konvoluce, jejíž forma má transponovanou matici $\boldsymbol{W}$.

- Pooling má za cíl zmenšit objem dat s využitím hodnot v blocích, např.
  - max pooling,
  - average pooling.

![Max pooling](assets/max-pooling.png)

- Často se aplikuje tzv. transfer learning, tedy:
  - layer freeze konvolučních vrstev, máme-li málo dat,
  - vyhození poslední FC vrstvy a nahrazení počtem tříd, které chceme my,
  - využití vlastních dat.

- Je mnoho úspěšných konvolučních sítí, např. AlexNet, VGG, GooGLeNet, ResNet, MobileNet, ...

![Alexnet](assets/alexnet.png)

- AlexNet má následující architekturu:

| Vrstva     |   Typ   |     Filtr     | Stride | Padding |    Výstup     | Parametry                              |
| :--------- | :-----: | :-----------: | :----: | :-----: | :-----------: | :------------------------------------- |
| **Vstup**  |    —    |       —       |   —    |    —    | 227 × 227 × 3 | 0                                      |
| **Conv1**  |  conv   | 11 × 11, F=96 |   4    |    0    | 55 × 55 × 96  | 96 · (11 · 11 · 3 + 1) = 34 944        |
| **Pool1**  | maxpool |     3 × 3     |   2    |    0    | 27 × 27 × 96  | 0                                      |
| **Conv2**  |  conv   | 5 × 5, F=256  |   1    |    2    | 27 × 27 × 256 | 256 · (5 · 5 · 96 + 1) = 614 656       |
| **Pool2**  | maxpool |     3 × 3     |   2    |    0    | 13 × 13 × 256 | 0                                      |
| **Conv3**  |  conv   | 3 × 3, F=384  |   1    |    1    | 13 × 13 × 384 | 384 · (3 · 3 · 256 + 1) = 885 120      |
| **Conv4**  |  conv   | 3 × 3, F=384  |   1    |    1    | 13 × 13 × 384 | 384 · (3 · 3 · 384 + 1) = 1 327 488    |
| **Conv5**  |  conv   | 3 × 3, F=256  |   1    |    1    | 13 × 13 × 256 | 256 · (3 · 3 · 384 + 1) = 884 992      |
| **Pool3**  | maxpool |     3 × 3     |   2    |    0    |  6 × 6 × 256  | 0                                      |
| **FC6**    |   fc    |       —       |   —    |    —    |     4096      | 6 · 6 · 256 · 4096 + 4096 = 37 752 832 |
| **FC7**    |   fc    |       —       |   —    |    —    |     4096      | 4096 · 4096 + 4096 = 16 781 312        |
| **FC8**    |   fc    |       —       |   —    |    —    |     1000      | 4096 · 1000 + 1000 = 4 097 000         |
| **Celkem** |         |               |        |         |               | **≈ 62.4M**                            |

- Výstupy z konvolučních sítí můžeme zobrazovat pomocí:
  - filtrů první vrstvy,
  - vizualizace nejbližšího souseda,
  - clusteringu,
  - zobrazení aktivací jednotlivých filtrů,
  - Class Activation Map,
  - generováním obrázků maximalizujících zadanou třídu.

## Rekurentní neuronové sítě

Rekurentní sítě pracují s vnitřním stavem z předchozího stavu sítě, mohou tedy modelovat výstupy s ohledem na předchozí vstupy v čase.

- Vnitřní stav neuronu vracíme zpátky do sítě: $\boldsymbol{h}_t = g(\boldsymbol{h}_{t-1}, \boldsymbol{x}_t)$.

- U RRNs se jako $g()$ používá $\tanh()$ s 3 různými váhovými maticemi:

  $$
  \boldsymbol{h}_t = \tanh(\boldsymbol{W}^{xh}\boldsymbol{x}_t+\boldsymbol{W}^{hh}\boldsymbol{h}_{t-1}+\boldsymbol{b}^h),\\
  \boldsymbol{s}_t=\boldsymbol{W}^{hs}\boldsymbol{h}_t+\boldsymbol{b}^s.
  $$

  ![RNN ht](assets/rnn-ht.png)
  ![RNN st](assets/rnn-st.png)
  ![RNN gradienty](assets/rnn-gradienty.png)

- Můžeme modelovat jazyk pomocí one-hot encodingu, kde největší hodnota u výstupu je vybrána jako nejvíce pravděpodobný znak v sekvenci.

- Celkový gradient se počítá jako součet jednotlivých gradientů v jednotlivých krocích: $\overline{\boldsymbol{W}^{hy}} = \sum_{t=1}^T\overline{\boldsymbol{W}^{hy}}_t.$

![RNN loss](assets/rnn-loss.png)

- Učení probíhá pomocí BPTT (backpropagation through time), tedy analogicky jako bychom rozvinuli rekurentní síť v čase.

![RNN backprop](assets/rnn-bptt.png)

### LSTM

Základní RNN mají problémy s vlivem mizejících nebo explodujících gradientů, řešením je tedy architektura LSTM (Long Short Term Memory).

$$
  \begin{align*}
  i_t = \sigma(\boldsymbol{W}^{xi}\boldsymbol{x}_t+\boldsymbol{W}^{hi}\boldsymbol{h}_{t-1})&\quad\text{input gate},\\
  f_t = \sigma(\boldsymbol{W}^{xf}\boldsymbol{x}_t+\boldsymbol{W}^{hf}\boldsymbol{h}_{t-1})&\quad\text{forget gate},\\
  o_t = \sigma(\boldsymbol{W}^{xo}\boldsymbol{x}_t+\boldsymbol{W}^{ho}\boldsymbol{h}_{t-1})&\quad\text{output gate},\\
  \tilde{c}_t = \tanh(\boldsymbol{W}^{xc}\boldsymbol{x}_t+\boldsymbol{W}^{hc}\boldsymbol{h}_{t-1})&\quad\text{update cell state},\\
  c_t = f_t \odot c_{t-1} + i_t \odot \tilde{c}_t&\quad\text{cell state},\\
  h_t = o_t \odot \tanh(c_t)&\quad\text{output}.
  \end{align*}
$$

### GRU

Je zjednodušení LSTM, protože má pouze jednu stavovou proměnnou $h_t$:

$$
\begin{align*}
z_t = \sigma(\boldsymbol{W}^{xz}\boldsymbol{x}_t+\boldsymbol{W}^{hz}\boldsymbol{h}_{t-1})\\
r_t = \sigma(\boldsymbol{W}^{xr}\boldsymbol{x}_t+\boldsymbol{W}^{hr}\boldsymbol{h}_{t-1})\\
\tilde{h}_t = \tanh(\boldsymbol{W}^{xh}\boldsymbol{x}_t+\boldsymbol{W}^{hh}(r_t \odot h_{t-1}))\\
h_t = (1-z_t) \odot h_{t-1} + z_t \odot \tilde{h}_t

\end{align*}
$$

![RNNs](assets/rnns.png)

### CTC

Connectionist Temporal Classification je metoda, kdy do RNN posíláme s konstantní frekvencí nějaké příznaky $\epsilon$ a v každém okamžiku pak predikujeme pravděpodobnost pro každý znak.

![CTC](assets/ctc.png)
